function findMaxNumber (a, b, c){
    if (b > a){
        a = b;
    }
    if ( c > a){
        a = c;
    }
    return a;
}

let maxNummer = findMaxNumber(16, 13, 4);
console.log("Наибольшее число:", maxNummer);
